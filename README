Magic Kard Market Python SDK
--------------------

# Setup

For the SDK to work properly you have to set four environment variables:
* MKM_APP_TOKEN
* MKM_APP_SECRET
* MKM_ACCESS_TOKEN
* MKM_ACCESS_TOKEN_SECRET

# Simple usage::

    mkm.account_management.account()

    mkm.market_place.user(user='SimpleUser')
